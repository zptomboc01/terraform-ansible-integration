# Code reference: https://spacelift.io/blog/github-actions-terraform

name: "Terraform Infrastructure CICD Pipeline"
 
on:
 push:
   branches:
   - main
   paths:
   - terraform/**
 pull_request:
   branches:
   - main
   paths:
   - terraform/**
 
env:
 # verbosity setting for Terraform logs
 TF_LOG: INFO
 # Credentials for deployment to AWS
 AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
 AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
 
jobs:
 validate:
   name: "Validation & Formatting"
   runs-on: ubuntu-latest
   defaults:
     run:
       shell: bash
       working-directory: ./terraform
   outputs:
     fmt-outcome: ${{ steps.fmt.outcome }}
     validate-outcome: ${{ steps.validate.outcome }}
 
   steps:
     - name: Checkout the repository to the runner
       uses: actions/checkout@v4
 
     - name: Setup Terraform with specified version on the runner
       uses: hashicorp/setup-terraform@v3
       with:
         terraform_version: latest

     - name: Cache Terraform providers
       uses: actions/cache@v3
       with:
         path: ~/.terraform.d/plugin-cache
         key: ${{ runner.os }}-terraform-${{ hashFiles('**/.terraform.lock.hcl') }}
         restore-keys: |
           ${{ runner.os }}-terraform-
    
     - name: Terraform init
       id: init
       run: terraform init
 
     - name: Terraform format check
       id: fmt
       run: terraform fmt -check -diff
    
     - name: Terraform validate
       id: validate
       run: terraform validate -json

 security-scan:
   name: "Security Scanning"
   runs-on: ubuntu-latest
   needs: validate
   permissions:
     security-events: write
     contents: read
   defaults:
     run:
       shell: bash
       working-directory: ./terraform
   outputs:
     checkov-outcome: ${{ steps.checkov.outcome }}
     tflint-outcome: ${{ steps.tflint.outcome }}
     ansible-lint-outcome: ${{ steps.ansible-lint.outcome }}

   steps:
     - name: Checkout the repository to the runner
       uses: actions/checkout@v4

     - name: Setup Python for security tools
       uses: actions/setup-python@v4
       with:
         python-version: '3.x'
         cache: 'pip'

     - name: Install security scanning tools
       run: |
         pip install checkov
         pip install ansible-lint

     - name: Run Checkov security scan
       id: checkov
       run: |
         checkov -d . --framework terraform --output cli --output sarif --output-file-path console,checkov-report.sarif
       continue-on-error: true

     - name: Upload Checkov SARIF results
       uses: github/codeql-action/upload-sarif@v3
       if: always()
       with:
         sarif_file: ./terraform/checkov-report.sarif
         category: checkov

     - name: Setup TFLint
       uses: terraform-linters/setup-tflint@v3
       with:
         tflint_version: v0.48.0
     
     - name: Initialize TFLint
       run: tflint --init

     - name: Run TFLint scan
       id: tflint
       run: tflint --format compact .
       continue-on-error: true

     - name: Run Ansible Lint
       id: ansible-lint
       run: ansible-lint test_playbook.yml --format sarif --output-file ansible-lint-report.sarif
       continue-on-error: true

     - name: Upload Ansible Lint SARIF results
       uses: github/codeql-action/upload-sarif@v3
       if: always()
       with:
         sarif_file: ./terraform/ansible-lint-report.sarif
         category: ansible-lint

     - name: Security scan summary
       if: always()
       run: |
         echo "## Security Scan Results" >> $GITHUB_STEP_SUMMARY
         echo "- Checkov: ${{ steps.checkov.outcome }}" >> $GITHUB_STEP_SUMMARY
         echo "- TFLint: ${{ steps.tflint.outcome }}" >> $GITHUB_STEP_SUMMARY
         echo "- Ansible Lint: ${{ steps.ansible-lint.outcome }}" >> $GITHUB_STEP_SUMMARY

 plan:
   name: "Terraform Plan"
   runs-on: ubuntu-latest
   needs: [validate, security-scan]
   if: github.event_name == 'pull_request'
   permissions:
     contents: read
     pull-requests: write
   defaults:
     run:
       shell: bash
       working-directory: ./terraform
   outputs:
     plan-outcome: ${{ steps.plan.outcome }}

   steps:
     - name: Checkout the repository to the runner
       uses: actions/checkout@v4
 
     - name: Setup Terraform with specified version on the runner
       uses: hashicorp/setup-terraform@v3
       with:
         terraform_version: latest

     - name: Cache Terraform providers
       uses: actions/cache@v3
       with:
         path: ~/.terraform.d/plugin-cache
         key: ${{ runner.os }}-terraform-${{ hashFiles('**/.terraform.lock.hcl') }}
         restore-keys: |
           ${{ runner.os }}-terraform-
    
     - name: Terraform init
       run: terraform init
 
     - name: Terraform plan
       id: plan
       run: |
         terraform plan -no-color -input=false -out=tfplan
         terraform show -no-color tfplan
       continue-on-error: true

     - name: Comment PR with plan results
       uses: actions/github-script@v6
       env:
         PLAN: "terraform\n${{ steps.plan.outputs.stdout }}"
       with:
         script: |
           const output = `#### Terraform Format and Style 🖌\`${{ needs.validate.outputs.fmt-outcome }}\`
           #### Terraform Initialization ⚙️\`success\`
           #### Terraform Validation 🤖\`${{ needs.validate.outputs.validate-outcome }}\`
           #### Security Scans 🔒
           - Checkov: \`${{ needs.security-scan.outputs.checkov-outcome }}\`
           - TFLint: \`${{ needs.security-scan.outputs.tflint-outcome }}\`
           - Ansible Lint: \`${{ needs.security-scan.outputs.ansible-lint-outcome }}\`
           #### Terraform Plan 📖\`${{ steps.plan.outcome }}\`
 
           <details><summary>Show Plan</summary>
 
           \`\`\`\n
           ${process.env.PLAN}
           \`\`\`
 
           </details>
           *Pushed by: @${{ github.actor }}, Action: \`${{ github.event_name }}\`*`;
 
           github.rest.issues.createComment({
             issue_number: context.issue.number,
             owner: context.repo.owner,
             repo: context.repo.repo,
             body: output
           })

     - name: Terraform Plan Status
       if: steps.plan.outcome == 'failure'
       run: exit 1

 deploy:
   name: "Terraform Apply"
   runs-on: ubuntu-latest
   needs: [validate, security-scan]
   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
   permissions:
     contents: read
   defaults:
     run:
       shell: bash
       working-directory: ./terraform
   environment:
     name: production
     url: ${{ steps.output.outputs.url }}

   steps:
     - name: Checkout the repository to the runner
       uses: actions/checkout@v4
 
     - name: Setup Terraform with specified version on the runner
       uses: hashicorp/setup-terraform@v3
       with:
         terraform_version: latest

     - name: Cache Terraform providers
       uses: actions/cache@v3
       with:
         path: ~/.terraform.d/plugin-cache
         key: ${{ runner.os }}-terraform-${{ hashFiles('**/.terraform.lock.hcl') }}
         restore-keys: |
           ${{ runner.os }}-terraform-
    
     - name: Terraform init
       run: terraform init

     - name: Check security scan results
       run: |
         if [ "${{ needs.security-scan.outputs.checkov-outcome }}" = "failure" ]; then
           echo "❌ Checkov found critical security issues. Deployment blocked."
           exit 1
         fi
         echo "✅ Security scans completed. Proceeding with deployment."
 
     - name: Terraform Apply
       id: apply
       run: terraform apply -auto-approve -input=false

     - name: Get outputs
       id: output
       run: |
         echo "url=http://$(terraform output -raw web_server_ip)" >> $GITHUB_OUTPUT
